# =============================================
# DATABASE CONFIGURATION - INTERNAL TOOLS TEST
# =============================================

# MySQL Configuration
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_PORT=

# PostgreSQL Configuration
POSTGRES_DATABASE=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_PORT=

# Admin Tools (optional - use with --profile admin-tools)
PHPMYADMIN_PORT=
PGADMIN_PORT=
PGADMIN_EMAI=
PGADMIN_PASSWORD=

# =============================================
# APPLICATION CONNECTION STRINGS (for candidates)
# =============================================

# MySQL Connection Examples
MYSQL_CONNECTION_STRING="mysql://user:password@localhost:port/dbname"
MYSQL_DSN="mysql:host=localhost;port=port;dbname=dbname;charset=utf8mb4"

# PostgreSQL Connection Examples
POSTGRES_CONNECTION_STRING="postgresql://user:password@localhost:port/dbname"
POSTGRES_DSN="pgsql:host=localhost;port=port;dbname=dbname"

# =============================================
# DEVELOPMENT NOTES
# =============================================
# Start MySQL only: docker-compose up mysql
# Start PostgreSQL only: docker-compose up postgres
# With admin tools: docker-compose --profile admin-tools up mysql phpmyadmin
# Reset everything: docker-compose down -v && docker-compose up
